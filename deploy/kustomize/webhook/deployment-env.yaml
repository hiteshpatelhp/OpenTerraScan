apiVersion: apps/v1
kind: Deployment
metadata:
  name: openterrascan
spec:
  template:
    spec:
      containers:
      - name: openterrascan-server
        env:
        - name: K8S_WEBHOOK_API_KEY
          value: <WEBHOOK_API_KEY>
