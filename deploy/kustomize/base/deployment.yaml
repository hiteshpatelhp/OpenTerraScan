apiVersion: apps/v1
kind: Deployment
metadata:
  name: openterrascan
spec:
  replicas: 1
  template:
    spec:
      initContainers:
        - name: git-cloner
          image: alpine/git
          args:
            - clone
            - --single-branch
            - --branch=master
            - https://github.com/tenable/openterrascan.git
            - /data
          volumeMounts:
            - mountPath: /data
              name: openterrascan-data-sync
      containers:
      - name: openterrascan-server
        image: tenable/openterrascan:1.16.0
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            memory: "256Mi"
            cpu: "0.5"
        ports:
        - containerPort: 9010
        livenessProbe:
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          httpGet:
            path: /health
            port: 9010
            scheme: http
        volumeMounts:
        - name: openterrascan-data-sync
          mountPath: /home/openterrascan/.openterrascan
        - mountPath: /data/config
          name: openterrascan-config
          readOnly: true
        command: ["/bin/sh", "-c"]
        args:
        - >
          openterrascan server -l debug -c /data/config/config.toml
      volumes:
        #add a configmap for the openterrascan config.toml file
        - name: openterrascan-config
          configMap:
            name: openterrascan-config
        - name: openterrascan-data-sync
          emptyDir: {}
